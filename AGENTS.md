# 에이전트 모델 라우팅

토큰과 비용을 아끼는 것을 기본 원칙으로 삼는다. 작업을 시작하기 전에 필요한 추론 강도를 판단하고 다음과 같이 모델을 선택한다.

- 복잡한 계획 수립, 아키텍처 결정, 모호한 요구사항 정리, 여러 파일에 걸친 디버깅, 보안·데이터 무결성처럼 판단 비용이 큰 작업은 사용 가능한 최상위 모델을 높은 추론 강도로 사용한다. 현재 기준: `gpt-5.6-sol` + `high` 이상.
- 파일 탐색, 상태 확인, 정형화된 수정, 포맷팅, 단순 명령 실행, 이미 정해진 계획의 반복 구현처럼 판단이 적은 작업은 저비용 모델을 낮은 추론 강도로 사용한다. 현재 기준: `gpt-5.6-terra` + `low`.
- 복합 작업은 최상위 모델이 계획과 검토를 맡고, 독립적인 단순 하위 작업만 저비용 모델에 위임한다.
- 위임 자체의 비용이 작업보다 크면 에이전트를 추가하지 말고 현재 에이전트가 바로 처리한다.
- 작업 난이도가 불확실하거나 잘못된 판단의 비용이 크면 상위 모델을 선택한다.
- 모델 목록이 바뀌면 특정 이름보다 위의 역할과 비용 원칙을 우선한다.

## 계산기 키패드 배열 — 변경 금지

Figma의 `02 · Calculator · Empty · Dark` 프레임에 정의된 다음 4열 × 4행 배열을 HOWMU 계산기의 고정 기준으로 사용한다.

| 1열 | 2열 | 3열 | 4열 |
| --- | --- | --- | --- |
| 1 | 2 | 3 | ÷ |
| 4 | 5 | 6 | × |
| 7 | 8 | 9 | − |
| . | 0 | 삭제 | + |

- 간격, 크기, 색상, 표면 처리 등 시각 디자인은 화면에 맞게 조정할 수 있다.
- 키의 순서, 행과 열, 연산자 위치, `삭제` 위치는 사용자가 명시적으로 변경을 요청하지 않는 한 절대 수정하지 않는다.
- 코드와 Figma의 모든 계산기 상태는 항상 이 배열을 동일하게 사용한다.
