---
name: HOWMU POC Design System
version: 2.0
status: implemented-baseline
updated: 2026-08-08
product: HOWMU
wordmark: howmu?
locale: ko-KR
reference: Apple-design-analysis
---

# HOWMU Design System

현재 HOWMU 모바일 웹 POC의 구현 기준이다. Apple DESIGN.md의 낮은 정보 밀도, 넓은 여백, 단일 파란색 액션 컬러, 그림자 없는 UI 원칙을 HOWMU 계산기 경험에 맞게 적용한다. 제품 범위는 `docs/PRD.md`를 따른다.

## 1. 디자인 원칙

- **Direct:** 첫 방문에는 여행지 통화 선택을, 이후에는 계산기를 즉시 보여준다. 설명용 시작 화면이나 홍보 문구를 앞에 두지 않는다.
- **Quiet:** 제품 사용을 방해하는 헤드라인, 장식, 중복 버튼을 제거한다.
- **One accent:** 동작과 선택은 Apple Action Blue 계열 하나로만 표현한다.
- **Low chrome:** 주요 영역은 흰색과 파치먼트 면, 얇은 경계선으로 구분한다. 카드와 내비게이션에 장식용 그림자를 쓰지 않는다.
- **Immediate:** 숫자를 누르면 저장된 환율로 결과가 즉시 갱신된다. 입력 중 네트워크 응답을 기다리지 않는다.
- **Mobile first:** iPhone safe area와 44px 이상의 터치 영역을 우선한다.

## 2. 브랜드

| 항목 | 표기 |
| --- | --- |
| 공식 서비스명 | `HOWMU` |
| 소비자용 워드마크 | `howmu?` |
| 한국어 호칭 | `하무` |
| 영문 메시지 | `Know before you pay.` |
| 한국어 메시지 | `결제하기 전에, 얼마인지부터.` |

워드마크는 소문자 `howmu`와 Action Blue 물음표를 결합한다. 계산 화면에서는 서비스 식별에만 사용하며 별도 설명 문구를 붙이지 않는다.

## 3. 화면 구조

### 첫 실행

1. `howmu?` 헤더
2. `여행지 통화` 제목과 한 줄 안내
3. 통화 검색
4. 자주 쓰는 여행지 통화 목록

별도 시작 버튼과 중간 온보딩 화면은 사용하지 않는다. 통화를 선택하면 즉시 계산기로 이동한다.

### 계산기

1. `howmu?` 헤더
2. 여행지 통화·입력 금액과 기준 통화·환산 결과를 묶은 환산 카드
3. 환산 카드 안의 환율 기준일 및 오프라인 상태
4. 4열 × 4행 숫자 패드
5. `계산기 / 마이` 하단 GNB

`이 가격, 얼마일까요?` 같은 설명형 헤드라인은 사용하지 않는다. 사용자가 화면을 여는 즉시 통화와 숫자 입력 영역을 볼 수 있어야 한다.

### 마이

1. 계정 연결 또는 게스트 상태
2. 여행지 통화와 기준 통화
3. 다크 모드와 환율 상태
4. 서비스 및 개인정보 안내

계산기 화면에 별도의 설정 버튼을 두지 않는다. 계정과 모든 설정은 `마이`에만 둔다.

## 4. 컬러

### Light

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `background` | `#F5F5F7` | Apple 파치먼트 캔버스 |
| `surface` | `#FFFFFF` | 카드와 입력 영역 |
| `surface-soft` | `#FAFAFC` | 눌림 전 보조 면 |
| `text` | `#1D1D1F` | 주요 텍스트와 결과 |
| `text-sub` | `#333333` | 보조 설명 |
| `text-dim` | `#7A7A7A` | 상태, 캡션 |
| `blue` | `#0066CC` | 선택과 동작 |
| `blue-focus` | `#0071E3` | 포커스 |
| `line` | `#E0E0E0` | 헤어라인 |

### Dark

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `background` | `#000000` | 앱 배경 |
| `surface` | `#272729` | 주요 카드 |
| `surface-raised` | `#2A2A2C` | 바텀 시트 |
| `surface-soft` | `#252527` | 보조 면 |
| `text` | `#FFFFFF` | 주요 텍스트 |
| `text-sub` | `#CCCCCC` | 보조 설명 |
| `blue` | `#2997FF` | 어두운 면의 동작 |

## 5. 타이포그래피

- 한국어: **Pretendard Variable**
- 영문, 숫자, 통화 코드, 연산 기호: **Inter Variable**
- 디스플레이 텍스트는 Apple식으로 자간을 좁히되 제품 화면의 큰 설명형 헤드라인은 최소화한다.
- 기본 본문은 17px, 보조 문구는 12–14px, 통화 코드는 19px, 입력 금액은 30–36px, 환산 결과는 23–29px를 사용한다.
- 금액에는 tabular numbers를 적용하고 오른쪽 정렬한다.

## 6. 간격과 형태

- 기준 프레임: `390 × 844px`
- 구현 최대 너비: `430px`
- 화면 좌우 여백: `18px`
- 간격 스케일: `4, 8, 12, 17, 24, 32, 48`
- 카드 반경: `18px`
- 작은 컨트롤: `8–11px`
- 원형·캡슐 컨트롤: `9999px`
- 바텀 시트 상단 반경: `28px`
- 카드와 GNB에는 그림자를 사용하지 않는다. 데스크톱의 모바일 프리뷰 외곽과 바텀 시트에만 최소한의 깊이를 허용한다.

## 7. 핵심 컴포넌트

### 환산 카드

- 첫 행: 여행지 통화와 입력 금액
- 둘째 행: 기준 통화와 환산 결과
- 두 행 사이: 통화 교환 버튼
- 카드 하단: 환율 기준일, 갱신 또는 오프라인 상태
- 입력 금액과 동작 요소만 파란색으로 표시한다.

### 숫자 패드

| 1열 | 2열 | 3열 | 4열 |
| --- | --- | --- | --- |
| 1 | 2 | 3 | ÷ |
| 4 | 5 | 6 | × |
| 7 | 8 | 9 | − |
| . | 0 | 삭제 | + |

개별 키에 상시 배경이나 구분선을 넣지 않는다. 눌렀을 때만 `pressed` 면을 표시한다. 연산자는 Action Blue, 숫자는 기본 텍스트 색을 사용한다.

### 하단 GNB

- `계산기`, `마이` 두 항목만 제공한다.
- 현재 화면은 Action Blue로 표시한다.
- 계산기 상단에 `마이`와 같은 중복 진입점을 만들지 않는다.
- 카메라가 실제로 구현되기 전에는 미동작 메뉴를 추가하지 않는다.

### 통화 선택

- 검색과 통화 목록을 바텀 시트로 제공한다.
- 통화 코드, 한국어 통화명, 영문 통화명을 함께 검색한다.
- 항목 전체가 최소 44px 이상의 터치 영역을 가진다.

## 8. 상태와 접근성

- 첫 방문 기본 테마는 라이트이며 사용자의 선택을 저장한다.
- 오프라인에서는 마지막 환율을 사용하고 기준일과 상태 문구를 함께 표시한다.
- 색상만으로 성공·경고 상태를 구분하지 않는다.
- 모든 동작 요소는 최소 44 × 44px 터치 영역을 가진다.
- 키보드 포커스는 2px Action Blue 외곽선으로 표시한다.
- 모션 감소 설정이 있으면 전환 시간을 사실상 제거한다.

## 9. POC 계정 정책

로그인하지 않아도 계산기와 모든 로컬 설정을 사용할 수 있다. Apple, Google, Kakao 연결 UI는 POC에서는 기기 저장 방식으로 동작하며 실제 OAuth 서버 연결은 정식 앱 단계에서 추가한다.

## 10. 제외 범위

- 카메라 권한과 실시간 OCR
- 상품 인식과 적정 가격 판정
- 가격 비교 데이터 수집
- GPS 자동 국가 인식
- 실제 OAuth와 서버 계정 동기화
- 미동작 카메라 탭과 미래 기능 메뉴
