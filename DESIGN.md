---
name: HOWMU POC Design System
version: 1.0
status: implemented-baseline
updated: 2026-08-08
product: HOWMU
wordmark: howmu?
locale: ko-KR
---

# HOWMU Design System

이 문서는 현재 구현된 HOWMU 모바일 웹 POC를 디자인 기준으로 정리한 문서다. Figma와 제품 구현은 이 문서를 공통 기준으로 삼는다. 제품 요구사항과 범위는 `docs/PRD.md`를 따른다.

## 1. 디자인 방향

HOWMU는 여행자가 가격표를 본 순간 계산을 끝낼 수 있어야 한다. 화면은 정보 밀도를 낮추고, 환산에 필요한 두 통화와 숫자 입력에만 시선을 모은다.

- **Dark first:** 기본은 검은 캔버스와 짙은 카드로 구성한다. 라이트 모드는 동일한 정보 구조를 유지한다.
- **Instant:** 입력 결과는 키를 누르는 즉시 바뀐다. 로딩 UI가 계산 흐름을 막지 않는다.
- **Card hierarchy:** 통화와 결과, 숫자 패드를 각각 큰 카드로 묶는다.
- **One accent:** 파란색은 교환, 연산, 선택, 주요 상태에만 사용한다.
- **Mobile first:** 한 손 조작과 iPhone safe area를 우선한다. 데스크톱에서는 모바일 화면을 중앙에 둔다.
- **POC focus:** 계산기 경험만 완성한다. 카메라, 프로필, GNB는 노출하지 않는다.

## 2. 브랜드

| 항목 | 표기 |
| --- | --- |
| 공식 서비스명 | `HOWMU` |
| 소비자용 워드마크 | `howmu?` |
| 한국어 호칭 | `하무` |
| 영문 메시지 | `Know before you pay.` |
| 한국어 메시지 | `결제하기 전에, 얼마인지부터.` |

워드마크는 소문자 `howmu`와 파란색 물음표를 결합한다. 별도 로고 이미지 없이 텍스트만으로 사용하며, 제품 화면에서는 장식보다 서비스 식별 역할을 우선한다.

## 3. 기준 화면과 레이아웃

- Figma 기준 프레임: **390 × 844 px**
- 구현 최대 너비: **430 px**
- 좌우 여백: **18 px**
- 상단·하단: `env(safe-area-inset-top)`, `env(safe-area-inset-bottom)` 반영
- 섹션 간 기본 간격: **12 px**
- 주요 카드 내부 여백: **20 px**
- 데스크톱: 모바일 앱을 중앙 정렬하고 바깥은 캔버스 색으로 채운다.

화면은 위에서부터 다음 순서로 구성한다.

1. 브랜드 헤더와 테마 전환
2. 화면 이름 또는 간단한 안내
3. 통화·환산 카드
4. 환율 상태
5. 숫자 패드

## 4. 컬러 토큰

### Dark theme — 기본

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `canvas` | `#050608` | 앱 배경 |
| `surface` | `#17181E` | 환산 카드, 숫자 패드 |
| `surface-soft` | `#1D1E24` | 보조 영역, 입력 영역 |
| `chip` | `#25272F` | 통화 배지, 작은 버튼 |
| `ink` | `#F1F2F6` | 주요 텍스트와 숫자 |
| `muted` | `#9A9DA7` | 설명, 보조 라벨 |
| `action` | `#3B86F7` | 물음표, 연산자, 선택 상태 |
| `action-soft` | `#1C365E` | 파란색 보조 배경 |
| `line` | `rgba(255,255,255,0.07)` | 경계선 |
| `pressed` | `#24262D` | 눌림 상태 |
| `warning` | `#F6A93B` | 오래된 환율, 오프라인 상태 |

### Light theme

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `canvas` | `#F1F2F6` | 앱 배경 |
| `surface` | `#FFFFFF` | 주요 카드 |
| `surface-soft` | `#F7F7FA` | 보조 영역 |
| `chip` | `#ECEEF3` | 배지, 작은 버튼 |
| `ink` | `#17181D` | 주요 텍스트와 숫자 |
| `muted` | `#767983` | 설명, 보조 라벨 |
| `action` | `#357FF3` | 강조와 선택 상태 |
| `action-soft` | `#E7F0FF` | 파란색 보조 배경 |
| `line` | `rgba(20,24,32,0.08)` | 경계선 |
| `pressed` | `#E7E9EF` | 눌림 상태 |
| `warning` | `#B96A00` | 오래된 환율, 오프라인 상태 |

## 5. 타이포그래피

언어에 따라 두 서체를 함께 사용한다.

- **한국어:** Pretendard
- **영문·숫자·통화 코드·연산 기호:** Inter
- 한 문장에 한국어와 영문이 섞이면 동일한 텍스트 레이어 안에서 글자 구간별로 서체를 적용한다.
- 브랜드 워드마크 `howmu?`, 금액, 환율, 통화 코드는 항상 Inter를 사용한다.
- 굵기는 두 서체에서 시각적으로 대응시킨다. `400 Regular`, `500 Medium`, `600 SemiBold`, `700 Bold`를 기준으로 한다.

| 역할 | 크기 / 행간 | 굵기 |
| --- | --- | --- |
| 워드마크 | 24 / 29 | 700 |
| 온보딩 헤드라인 | 36 / 43 | 700 |
| 화면 라벨 | 12 / 16 | 600 |
| 통화 코드 | 20 / 25 | 500 |
| 입력 금액 | 30–36 / 40 | 650 |
| 환산 결과 | 23–29 / 34 | 650 |
| 숫자 패드 | 36 / 44 | 400 |
| 버튼·목록 | 16 / 22 | 600 |
| 도움말·상태 | 11–12 / 17 | 500 |

금액은 오른쪽 정렬하며 자릿수가 늘어날수록 36 px에서 30 px까지 자연스럽게 줄인다. 통화 단위와 보조 설명은 금액보다 한 단계 낮은 대비를 사용한다. 숫자 폭과 정렬의 일관성을 위해 금액과 숫자 패드에는 Inter를 고정한다.

## 6. 간격, 모서리, 그림자

- 간격 스케일: `4, 8, 12, 16, 18, 20, 24, 28, 32`
- 작은 컨트롤: `14–18 px` radius
- 카드: `28 px` radius
- 바텀 시트 상단: `32 px` radius
- 완전한 원형 버튼과 배지: `999 px` radius
- Dark card shadow: `0 14px 34px rgba(0,0,0,0.42)`
- Light card shadow: `0 12px 32px rgba(27,31,40,0.10)`

## 7. 핵심 컴포넌트

### 7.1 브랜드 헤더

- 왼쪽: `howmu?` 워드마크
- 오른쪽: 44 × 44 px 테마 전환 버튼
- 아이콘은 다크 모드에서 해, 라이트 모드에서 달을 표시한다.
- 헤더 자체에는 카드 배경이나 하단 구분선을 넣지 않는다.

### 7.2 화면 라벨

`환율 계산기`처럼 현재 기능을 짧게 표시한다. 칩처럼 보이되 시선 우선순위는 낮게 유지한다.

### 7.3 환산 카드

- 카드 반경: 28 px
- 두 개의 통화 행을 세로로 배치한다.
- 첫 행: 여행지 통화와 사용자가 입력한 금액
- 둘째 행: 기준 통화와 환산 결과
- 통화 행 왼쪽에는 원형 국기/통화 배지와 3자리 통화 코드를 둔다.
- 금액은 오른쪽 정렬하며 입력값은 파란색, 결과는 기본 잉크 색을 사용한다.
- 두 행 사이에는 원형 교환 버튼을 배치한다.
- 교환 버튼을 누르면 여행지 통화와 기준 통화가 서로 바뀐다.

### 7.4 환율 상태

환율 기준일과 연결 상태를 한 줄로 표시한다.

- 최신: `1 THB = 42.97 KRW · 2026. 8. 8.`
- 갱신 중: 저장된 값으로 계산을 유지하고 작은 진행 상태만 표시
- 오프라인/오래됨: 경고색 점과 `저장된 환율 사용 중` 문구 표시
- 오류가 있어도 마지막 환율이 있으면 계산기는 계속 동작한다.

### 7.5 숫자 패드

현재 구현은 참고 이미지와 동일한 **4열 × 4행** 구조다.

| 1열 | 2열 | 3열 | 4열 |
| --- | --- | --- | --- |
| 1 | 2 | 3 | ÷ |
| 4 | 5 | 6 | × |
| 7 | 8 | 9 | − |
| . | 0 | 삭제 | + |

- 숫자와 소수점은 잉크 색, 연산자는 파란색을 사용한다.
- `삭제`는 숫자보다 작게 표시한다.
- 각 키의 터치 영역은 최소 64 × 64 px, 실제 버튼 높이는 72 px 이상으로 설계한다.
- 키 사이에 선이나 개별 카드 배경을 넣지 않는다. 패드 전체가 하나의 큰 카드다.
- 누르는 동안 배경만 짧게 나타나며 크기는 `0.96`까지 축소할 수 있다.
- 연산자는 현재 입력 흐름을 보조하며 환산 결과는 항상 즉시 갱신된다.

### 7.6 통화 선택 바텀 시트

- 화면 하단에서 올라오는 시트이며 상단 모서리는 32 px다.
- 상단에 드래그 핸들, 제목, 닫기 버튼을 둔다.
- 검색 필드는 최소 48 px 높이로 통화 코드·국가·통화명을 검색한다.
- 목록 항목에는 국기/배지, 통화 코드, 통화명, 선택 표시가 들어간다.
- 뒤의 계산기 화면에는 반투명 검은 오버레이를 적용한다.

### 7.7 첫 실행 설정

- `Know before you pay.`를 작은 브랜드 메시지로 표시한다.
- 헤드라인은 `여행지 통화를 선택하세요`처럼 한 가지 행동만 요청한다.
- 기준 통화는 KRW로 미리 설정한다.
- 검색 필드와 자주 쓰는 통화 목록으로 여행지 통화를 선택한다.
- 선택이 끝나면 별도 복잡한 설정 없이 계산기 화면으로 이동한다.

## 8. Figma 화면 세트

한 페이지에 다음 프레임을 왼쪽에서 오른쪽 순서로 만든다.

1. `01 · Onboarding · Dark`
2. `02 · Calculator · Empty · Dark`
3. `03 · Calculator · 430 THB · Dark`
4. `04 · Currency Picker · Dark`
5. `05 · Offline · Stale Rate · Dark`
6. `06 · Calculator · 430 THB · Light`

대표 과제 화면은 `430 THB → 약 18,478 KRW` 예시를 사용한다. 실제 제품의 금액은 저장된 최신 환율을 기준으로 계산하며, 디자인의 숫자는 레이아웃 검증용이다.

각 프레임은 Auto Layout으로 구성하고 레이어 이름은 기능을 알 수 있게 작성한다. 예: `Header`, `Exchange Card`, `Travel Currency Row`, `Home Currency Row`, `Rate Status`, `Keypad`, `Currency Sheet`.

## 9. 상태와 상호작용

- 첫 방문의 기본 테마는 다크이며 사용자의 선택을 저장한다.
- 테마 전환은 레이아웃을 바꾸지 않고 토큰만 교체한다.
- 통화나 환율이 저장되어 있으면 앱 시작 즉시 먼저 표시한다.
- 숫자 입력 중에는 네트워크 요청을 하지 않는다.
- 입력과 환산 결과의 시각적 갱신 목표는 100 ms 이하다.
- 빈 입력, 중복 소수점, 정수 12자리, 소수 2자리 제한을 안전하게 처리한다.
- 모든 동작 요소는 최소 44 × 44 px 터치 영역을 가진다.
- 키보드 포커스는 파란색 2 px 외곽선으로 명확히 표시한다.
- 색상만으로 오프라인·오류 상태를 구분하지 않고 문구를 함께 사용한다.

## 10. POC 범위 밖

아래 항목은 PRD의 향후 제품 방향으로만 유지하며 이번 디자인 프레임에는 넣지 않는다.

- 계산기·카메라·프로필 3개 GNB
- 카메라 권한과 실시간 OCR
- 상품 인식 및 적정 가격 판정
- 마이페이지, 로그인, 여행 기록
- GPS와 자동 국가 인식
- 미동작 탭, 카메라 버튼, 미래 기능 안내 UI

## 11. 구현과 PRD의 차이

현재 구현을 디자인 기준으로 삼기 때문에 초기 PRD의 `고대비 밝은 테마`는 **다크 기본 + 라이트 전환**으로 확장되었다. 또한 초기 PRD의 `00·전체 삭제 중심 숫자 패드`는 사용자가 제공한 참고 레이아웃에 따라 **4열 연산자형 숫자 패드**로 변경되었다. 제품 범위와 즉시 환산·오프라인 원칙은 그대로 유지한다.
