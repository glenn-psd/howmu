---
name: HOWMU Product Design System
version: 2.2
status: product-flow-baseline
updated: 2026-08-13
product: HOWMU
wordmark: howmu?
locale: ko-KR
reference: Toss UX hierarchy adapted for HOWMU dark UI
reference_url: https://wwit.design/2021/02/16/toss/
---

# HOWMU Design System

HOWMU 모바일 제품과 웹 POC가 공유하는 디자인 기준이다. Toss 화면에서 관찰한 큰 정보 위계, 넓은 여백, 묶인 목록, 한 화면 한 행동을 참고하되 Toss의 브랜드, 색상, 내비게이션, 그래픽을 복제하지 않는다. HOWMU의 다크·보더리스 표면, 파란색 액션, Pretendard와 Inter 조합은 유지한다. 제품 범위와 단계는 `docs/PRD.md`를 따른다.

## 1. 디자인 원칙

- **Direct:** 화면을 열면 현재 과업과 다음 행동을 바로 이해할 수 있어야 한다.
- **One focus:** 한 화면에서 가장 중요한 상태, 금액 또는 질문 하나만 가장 강하게 강조한다.
- **Section first:** 독립 정보군은 섹션 제목과 충분한 여백으로 나누고, 같은 정보군은 가까이 묶는다.
- **Grouped lists:** 설정, 근거, 상점, 입력 항목은 독립 카드의 벽으로 만들지 않고 하나의 목록 그룹처럼 읽히게 한다.
- **One primary action:** 화면의 주요 CTA는 하나만 사용한다. 보조 행동은 텍스트 액션이나 낮은 위계의 버튼으로 둔다.
- **Quiet surfaces:** 보더와 장식용 그림자를 사용하지 않는다. 배경과 surface의 명도 차이, 여백, 타이포그래피로 계층을 만든다.
- **Immediate:** 계산기 숫자 입력은 저장된 환율로 즉시 반영하며 네트워크 응답을 기다리지 않는다.
- **Mobile first:** `375 × 812px`을 기준으로 설계하고 iPhone safe area와 44px 이상의 터치 영역을 우선한다.

## 2. 브랜드

| 항목 | 표기 |
| --- | --- |
| 공식 서비스명 | `HOWMU` |
| 소비자용 워드마크 | `howmu?` |
| 한국어 호칭 | `하무` |
| 영문 메시지 | `Know before you pay.` |
| 한국어 메시지 | `결제하기 전에, 얼마인지부터.` |

워드마크는 서비스 식별에 필요한 크기로만 사용한다. 기능 화면에는 설명형 브랜드 문구를 반복하지 않는다. 사용자가 해야 할 행동과 현재 상태가 브랜드보다 먼저 읽혀야 한다.

## 3. 제품 화면 구조

### 01–05 · 계산기 POC

첫 실행은 여행지 통화를 검색·선택한 뒤 즉시 계산기로 이동한다. 계산기에는 환산 카드, 상태 정보, 고정 키패드만 둔다. 설명용 시작 화면, 하단 GNB, 미동작 미래 기능은 노출하지 않는다.

### 06–07 · 준비와 권한

`상태 또는 상징 → 큰 제목 → 두 줄 이내 설명 → 필요한 보조 안내 → 단일 CTA` 순서로 구성한다.

- 헤더 아래 첫 콘텐츠까지 `24px`을 둔다.
- 큰 제목은 최대 두 줄, 설명은 최대 두 문장으로 제한한다.
- 체크리스트와 개인정보 안내는 하나의 보조 surface만 사용한다.
- CTA는 항상 화면 하단 safe area 위에 둔다.

### 08–12 · 카메라

카메라 장면을 full-bleed 핵심 콘텐츠로 사용한다. 헤더, 가격 오버레이, 하단 촬영 영역 또는 바텀 시트만 장면 위에 겹친다.

- 카메라 헤더의 좌우 기준선은 `16px`이다.
- 인식 가격은 화면 중앙 근처에 하나만 강조한다.
- 검토·분석·미인식 바텀 시트는 같은 폭, 반경, 손잡이, 내부 여백 규칙을 공유한다.
- 바텀 시트 내부 순서는 `handle → 상태/제목 → 설명 → CTA`로 단순화한다.

### 13–18 · 가격 판단과 제보

결과 화면은 `상품 요약 → 핵심 판단과 금액 → 보조 신뢰 정보 → 다음 행동` 순서다.

- `Price Status`만 가장 강한 surface로 사용한다.
- 신뢰도와 팁은 더 낮은 surface 또는 정보 행으로 처리한다.
- 판단 근거, 주변 판매가, 제보 필드는 동일한 행 규칙으로 묶는다.
- 같은 목록의 행 간격은 `8px`, 결과 영역과 다음 섹션 사이는 `24px`이다.

### 19–24 · 마이, 로그인, 여행 및 설정

Toss의 전체·설정 화면처럼 `페이지 헤더 → 계정/여행 요약 → 섹션 제목 → 묶인 목록` 구조를 사용한다.

- 로그인하지 않아도 핵심 기능을 사용할 수 있음을 먼저 밝힌다.
- 계정 안내는 일반 Tip Card가 아니라 계정 상태를 직접 보여주는 요약 영역으로 표현한다.
- 섹션 제목과 목록 사이에는 `8–12px`, 서로 다른 섹션 사이는 `24–32px`을 둔다.
- 설정 목록은 보더 없이 surface와 여백으로만 구분한다.
- 기록 화면은 날짜 라벨 아래 같은 유형의 기록 행을 한 그룹으로 묶는다.

06–24는 향후 전체 제품 흐름을 검증하기 위한 Figma 설계 범위다. 이 문서가 카메라, 분석, 로그인 또는 서버 기능의 현재 POC 구현을 의미하지는 않는다.

## 4. 컬러와 표면

### Light

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `background` | `#F5F5F7` | 앱 캔버스 |
| `surface` | `#FFFFFF` | 기본 카드와 행 |
| `surface-raised` | `#FFFFFF` | 바텀 시트 |
| `surface-soft` | `#FAFAFC` | 낮은 위계의 보조 면 |
| `text` | `#1D1D1F` | 주요 텍스트와 금액 |
| `text-sub` | `#515154` | 보조 설명 |
| `text-dim` | `#7A7A80` | 메타데이터와 캡션 |
| `blue` | `#0066CC` | 선택과 동작 |
| `blue-focus` | `#0071E3` | 포커스와 활성 상태 |

### Dark

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `background` | `#000000` | 앱 배경 |
| `surface` | `#17191C` | 주요 카드와 목록 행 |
| `surface-raised` | `#212327` | 바텀 시트와 강조 보조 면 |
| `surface-soft` | `#15171A` | 낮은 위계의 보조 면 |
| `text` | `#FFFFFF` | 주요 텍스트와 금액 |
| `text-sub` | `#CCCCCC` | 보조 설명 |
| `text-dim` | `#9A9AA0` | 메타데이터와 캡션 |
| `blue` | `#2997FF` | 동작, 선택, 핵심 상태 |

- 카드, 목록, 입력, 바텀 시트에 상시 보더를 넣지 않는다.
- 계층은 `background → surface → surface-raised`의 면 차이로 만든다.
- 그림자는 제품 화면에서 사용하지 않는다. 바텀 시트도 surface 차이와 상단 반경으로 분리한다.
- 파란색은 주요 행동, 선택, 핵심 수치에만 사용한다.

## 5. 타이포그래피

- 한국어: **Pretendard Variable**
- 영문, 숫자, 통화 코드, 연산 기호: **Inter Variable**
- 페이지 제목: `28/34px`, Bold 또는 Semibold
- 핵심 판단·금액: `32–40/44px`, Inter Semibold, tabular numbers
- 섹션 제목: `17–20/24–26px`, Pretendard Semibold
- 목록 제목과 내비게이션: `17/22px`, Pretendard Semibold
- 본문: `15–17/22–24px`, Pretendard Regular
- 보조 정보: `14/20px`
- 캡션과 메타데이터: `12–13/18px`
- 금액과 trailing value는 오른쪽 정렬한다.
- 설명보다 상태와 행동을 먼저 쓰고, 한 화면의 본문은 최대 두 문장으로 제한한다.

## 6. 간격과 형태

### 화면 기준

- 기준 프레임: `375 × 812px`
- 기본 좌우 여백: `16px`
- 기본 콘텐츠 폭: `343px`
- 상단 콘텐츠 시작: `20px`
- 하단 CTA: `x 16 / y 728 / w 343 / h 52px`
- CTA 아래 기본 안전 여백: `32px`
- 실제 구현: `padding-bottom: max(32px, env(safe-area-inset-bottom))`

### 수직 리듬

| 관계 | 간격 |
| --- | ---: |
| 같은 목록의 행 사이 | `8px` |
| 제목과 바로 관련된 설명 | `8–12px` |
| 섹션 제목과 콘텐츠 그룹 | `8–12px` |
| 관련 블록 사이 | `16px` |
| 독립 섹션 사이 | `24px` |
| 큰 화면 전환 또는 계정/설정 그룹 사이 | `32px` |

- 화면 전체에 동일한 `8px`을 반복하지 않는다. 간격 자체가 정보 관계를 설명해야 한다.
- 목록 행 높이는 `60–68px`, 로그인 제공자 버튼과 CTA는 `52px`을 기준으로 한다.
- 카드 내부 여백은 `16–20px`, 바텀 시트 좌우 내부 여백은 `16px`이다.
- 일반 카드 반경 `18px`, 목록 행 `11px`, 바텀 시트 상단 `28px`, 캡슐 `9999px`을 사용한다.

## 7. 핵심 컴포넌트

### 페이지 헤더

- 기본 컨테이너는 `343 × 44–50px`이다.
- 뒤로가기 화면의 제목은 화면 중심에 정렬한다.
- 루트 화면은 Brand와 짧은 페이지 이름만 보여준다.
- 헤더 아래 첫 콘텐츠는 역할에 따라 `8px` 또는 `24px` 뒤에 시작한다.

### 섹션과 목록

- 섹션 제목은 다음 그룹의 의미를 설명하며 목록과 `8–12px` 떨어진다.
- 목록 행은 `label + value + chevron/toggle` 구조를 공통 사용한다.
- 동일 그룹 행은 `8px` 간격으로 묶고, 다른 그룹과는 최소 `24px`을 둔다.
- 행마다 다른 보더나 그림자를 사용하지 않는다.

### 하단 CTA

- 한 화면의 주요 CTA는 하나만 둔다.
- 기본 크기는 `343 × 52px`, 좌우 `16px`, 화면 하단에서 `32px` 위다.
- 활성 CTA는 Action Blue와 흰색 레이블을 사용한다.
- 비활성 상태는 색상뿐 아니라 문구 또는 상태로도 이유를 전달한다.

### 카메라 바텀 시트

- 화면 폭 `375px`, 좌우 콘텐츠 `16px`, 상단 반경 `28px`을 사용한다.
- handle은 `36 × 4px`, 상단 `16px`, 가로 중앙에 둔다.
- 제목과 설명은 각각 한 정보만 전달한다.
- CTA가 있는 시트는 항상 `y 728–780px`에 버튼을 맞춘다.

### 환산 카드

- 첫 행: 여행지 통화와 입력 금액
- 둘째 행: 기준 통화와 환산 결과
- 두 행 사이: 통화 교환 버튼
- 카드 하단: 환율 기준일, 갱신 또는 오프라인 상태
- 입력 금액과 동작 요소만 파란색으로 표시한다.

### 숫자 패드

| 1열 | 2열 | 3열 | 4열 |
| --- | --- | --- | --- |
| 1 | 2 | 3 | ÷ |
| 4 | 5 | 6 | × |
| 7 | 8 | 9 | − |
| . | 0 | 삭제 | + |

개별 키에 상시 배경이나 구분선을 넣지 않는다. 눌렀을 때만 `pressed` 면을 표시한다. 연산자는 Action Blue, 숫자는 기본 텍스트 색을 사용한다.

### 통화 선택

- 검색과 통화 목록을 바텀 시트로 제공한다.
- 통화 코드, 한국어 통화명, 영문 통화명을 함께 검색한다.
- 항목 전체가 최소 44px 이상의 터치 영역을 가진다.

## 8. 카피 원칙

- 상태나 행동을 문장 앞에 둔다: `인터넷 연결이 필요해요`, `카메라를 켜세요`, `가격을 확인하고 있어요`.
- 기능 설명보다 사용자가 얻는 결과를 말한다: `주변 가격과 비교했어요`.
- 제목은 최대 두 줄, 본문은 최대 두 문장, 버튼은 가능한 한 동사 하나로 쓴다.
- 같은 사실을 카드 제목, 본문, CTA에서 반복하지 않는다.
- 서비스명과 태그라인을 기능 화면마다 반복하지 않는다.

## 9. 상태와 접근성

- 오프라인에서는 마지막 환율을 사용하고 기준일과 상태 문구를 함께 표시한다.
- 색상만으로 성공·경고·비활성 상태를 구분하지 않는다.
- 모든 동작 요소는 최소 `44 × 44px`, 목록 행은 가능하면 `60px` 이상의 터치 영역을 가진다.
- 웹 키보드 포커스는 `2px` Action Blue 외곽선으로 표시한다.
- 동적 텍스트가 늘어날 수 있는 제목과 본문은 고정 높이보다 오토레이아웃을 우선한다.
- 모션 감소 설정이 있으면 전환 시간을 사실상 제거한다.

## 10. 제품 범위와 내비게이션

- 현재 구현 POC는 계산기와 통화 선택이 핵심이며 로그인 없이 사용할 수 있다.
- 06–24의 카메라, 가격 판단, 마이, 로그인, 여행 설정 화면은 전체 제품 흐름 검증을 위한 설계다.
- 카메라·프로필 기능이 실제 구현되기 전에는 계산기 화면에 하단 GNB를 추가하지 않는다.
- 인증은 선택 사항이며, 비로그인 사용자의 계산·카메라 진입을 막지 않는다.
- GPS 자동 국가 인식, 실제 OCR·상품 인식, 가격 데이터 수집, OAuth·서버 동기화는 각각 구현 단계에서 별도 검증한다.
